describe('Tests in demo.test file', () => {
    test('should be equal two strings', () => {

    const message = 'Hello World'; 
    const message2 = `Hello World`;

    expect( message ).toBe( message2);

})
})
